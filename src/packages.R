# installing 3 packages
# there are n -number of variables in a dataset and classes
# in a single package
install.packages('dplyr')
install.packages('cluster')
install.packages('devtools')

# checking for specific packages if not found
# error will be displayed in the console
library('ggplot2')
library('utils')

# shows all the implicitly installed packages of r
library()

# checks for the specific package if not found
# warning message will be displayed in the console
# if the package is found it will the load the package
require('dplyr')


